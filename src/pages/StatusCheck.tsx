import type {NextPage} from 'next';
import {StatusCheck as StatusCheckContainer} from 'containers/StatusCheck';

const StatusCheck: NextPage = () => <StatusCheckContainer />;
export default StatusCheck;
